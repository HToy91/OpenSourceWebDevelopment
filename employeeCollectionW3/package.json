{
  "name": "employeecollectionw3",
  "version": "1.0.0",
  "description": "npm init npm install express --save npm install --save-dev nodemon npm install dotenv --save npm install mongoose --save",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "express-handlebars": "^8.0.4",
    "method-override": "^3.0.0",
    "mongoose": "^9.1.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}
